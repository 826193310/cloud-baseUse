server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.58.128 # nacos 服务注册中心地址
      config:
        server-addr: 192.168.58.128 # nacos 作为配置中心的地址
        file-extension: yaml # 指定 yaml 格式配置
        group: test # 配置文件的分组

# ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file.extension}

